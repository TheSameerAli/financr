import { KFormatterPipe } from './k-formatter.pipe';

describe('KFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new KFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
